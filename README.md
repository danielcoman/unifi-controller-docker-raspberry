# unifi-controller-docker-raspberry

Run ubiquiti unifi controller in docker on raspberry pi

Image Build:
```
docker-compose -f  build.yml build
sudo docker-compose up -d
```

Run:
```
sudo docker-compose up -d
```
